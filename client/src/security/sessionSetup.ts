// import { SignalProtocolAddress, SessionBuilder, SessionCipher } from "@privacyresearch/libsignal-protocol-typescript";

// async function setupSession(remoteUserId: string, preKeyBundle: any, store: any) {
//     const address = new SignalProtocolAddress(remoteUserId, 1);
//     const sessionBuilder = new SessionBuilder(store, address);

//     await sessionBuilder.processPreKey(preKeyBundle);

//     const sessionCipher = new SessionCipher(store, address);
//     return sessionCipher;
// }

// export { setupSession };
